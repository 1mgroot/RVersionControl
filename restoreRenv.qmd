---
title: "restoreRenv"
format: html
editor: visual
jupyter: python3
---

## Step 5: Restore the Environment (Optional)

To recreate an environment based on the `renv.lock` file, use:

``` r
renv::restore()
```

This ensures all packages and their versions match the lock file.

```{r}
renv::restore()
```
